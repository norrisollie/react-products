{
  "name": "react-login",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dart-sass": "^1.25.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0",
    "sass": "^1.77.6",
    "semver": "^7.6.2"
  },
  "engines": {
    "node": "16"
  },
  "engineStrict": true,
  "scripts": {
    "start": "source ~/.nvm/nvm.sh && nvm use && react-scripts start",
    "build": "source ~/.nvm/nvm.sh && nvm use && react-scripts build",
    "test": "source ~/.nvm/nvm.sh && nvm use && react-scripts test",
    "eject": "source ~/.nvm/nvm.sh && nvm use && react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
